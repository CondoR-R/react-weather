import type React from "react";
import Header from "../parentComponents/Header/Header";

const App: React.FC = () => {
  return (
    <div className="app">
      <Header />
    </div>
  );
};

export default App;
